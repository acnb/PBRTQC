% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advancedPBRTQC.R
\name{makeRegAdjEMA}
\alias{makeRegAdjEMA}
\title{Factory for regression-adjusted PBRTQC}
\usage{
makeRegAdjEMA(offsetDataStart = 120, offsetDataStop = 20, recalculate = 20)
}
\arguments{
\item{offsetDataStart}{Days of history before the current window (default: 120)}

\item{offsetDataStop}{Days before the current window where history stops (default: 20)}

\item{recalculate}{Recalculate model every this many days (default: 20)}
}
\value{
A list with:
\describe{
\item{\code{fn}}{Algorithm function to pass to \code{simPBRTQC()}}
\item{\code{getStore}}{Returns cached models for \code{analyse_models()}}
}
}
\description{
Creates an isolated instance of the regAdjEMA algorithm with its own
model cache. Each call returns a fresh instance, so Shiny sessions and
scripts stay independent with no shared global state.
}
