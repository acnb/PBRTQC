% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advancedPBRTQC.R
\name{makeRunLengthCounter}
\alias{makeRunLengthCounter}
\title{Factory for run-length counter PBRTQC}
\usage{
makeRunLengthCounter(baseFn)
}
\arguments{
\item{baseFn}{A PBRTQC algorithm function with the standard signature
\code{function(measurement, blockSize, ll, ul, dataExtra = NULL)}.}
}
\value{
A list with:
\describe{
\item{\code{fn}}{Algorithm function suitable for \code{simPBRTQC()}}
\item{\code{getStore}}{Returns an empty list (no internal state)}
}
}
\description{
Wraps a base PBRTQC function (such as \code{rollMed} or
\code{\link{truncatedEMA}}) and counts consecutive violations on the same
side of the control limits.  A switch from above the upper limit to below
the lower limit (or vice versa) resets the counter to 1.  Values within
the limits return 0.  NA values (e.g. warm-up period) propagate as NA and
also reset the run state.
}
\details{
The \code{ll} and \code{ul} arguments are forwarded to \code{baseFn} and
are also used as the lower and upper thresholds for run-length counting.
}
